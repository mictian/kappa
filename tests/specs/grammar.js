/*global describe:true it:true expect:true */
define(['../../src/data/grammar'], function(g)
{
	describe('Symbol', function ()
	{
		it('should not create a special one by default', function()
		{
			expect(0).toBe(0);
		});
	});
});